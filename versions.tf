terraform {
  # Import blocks (imports.tf) require Terraform 1.5+.
  required_version = ">= 1.5.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 6.0"
    }
  }
}
